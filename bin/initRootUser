#!/usr/bin/env bash
docker-compose exec db mysql -uroot -p"root" -e "CREATE USER 'root'@'%' IDENTIFIED BY 'root';"
docker-compose exec db mysql -uroot -p"root" -e "GRANT ALL PRIVILEGES ON * . * TO 'root'@'%';"
